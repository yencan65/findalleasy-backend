# FindAllEasy Backend — example environment variables
# Copy to .env and fill values. NEVER commit your real .env.

# Server
PORT=5050
NODE_ENV=development

# Database (any of these names are accepted)
MONGO_URI=mongodb://localhost:27017/findalleasy
# MONGODB_URI=mongodb://...

# Optional: override DB name
MONGO_DB=findalleasy

# CORS (comma-separated). Example:
# FRONTEND_ORIGINS=https://findalleasy.com,https://www.findalleasy.com,https://findalleasy.pages.dev
FRONTEND_ORIGINS=

# Affiliate / signing
AFFILIATE_SIGNING_SECRET=dev

# Affiliate providers (examples)
TRENDYOL_SUPPLIER_ID=
TRENDYOL_USERNAME=
TRENDYOL_PASSWORD=
HEPSIBURADA_AFFILIATE_ID=
HEPSIBURADA_AFFILIATE_SECRET=

# AI providers (optional)
OPENAI_API_KEY=
GEMINI_API_KEY=
MISTRAL_API_KEY=

# Vision (camera/file image -> query)
# Cloud Vision key (monthly free quota may apply depending on GCP account)
GOOGLE_VISION_API_KEY=

# SerpApi Lens (PAID) — default OFF; enable only if you want paid fallback
SERPAPI_KEY=
VISION_ALLOW_SERP_LENS=0

# Free vision route: /api/vision/free (server-side Tesseract OCR)
USE_FREE_VISION=1
TESSERACT_LANG=tur+eng
# Optional: override tesseract language data location (CDN or local path)
# TESSERACT_LANG_PATH=https://tessdata.projectnaptha.com/4.0.0
VISION_FREE_TIMEOUT_MS=9000
VISION_FREE_CACHE_TTL_MS=3600000
VISION_FREE_CACHE_MAX=400

# Web Push (optional)
VAPID_PUBLIC=
VAPID_PRIVATE=

# Optional build metadata (used by /api/version)
BUILD_SHA=
# Email (Activation / Password Reset)
# -----------------------------------------------------------------------------
# Minimal (Gmail App Password recommended):
#   EMAIL_USER=your_gmail@gmail.com
#   EMAIL_PASS=xxxx xxxx xxxx xxxx   # App Password (spaces OK; we strip)
# Optional explicit SMTP (defaults to smtp.gmail.com:465 for gmail):
#   EMAIL_HOST=smtp.gmail.com
#   EMAIL_PORT=465
#   EMAIL_SECURE=1
# Optional:
#   MAIL_FROM=FindAllEasy <your_gmail@gmail.com>
#   MAIL_DEBUG=
MAIL_TLS_INSECURE=1        # logs mail attempts
#   MAIL_TLS_INSECURE=1 # last resort if SMTP TLS errors
#   MAIL_SANDBOX=1      # does NOT send real email; logs to console
EMAIL_USER=
EMAIL_PASS=
EMAIL_HOST=
EMAIL_PORT=
EMAIL_SECURE=
MAIL_FROM=
MAIL_DEBUG=
MAIL_TLS_INSECURE=
MAIL_SANDBOX=
